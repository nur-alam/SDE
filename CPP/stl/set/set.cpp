// #include <bits/stdc++.h>
#include <iostream>
#include <vector>
#include<algorithm>
#include<set>
#include<map>
#include<stack>
#include<unordered_set>
using namespace std;

int main() {
    map<int, int> mp;
    // mp[1] = 12;
    // cout << mp
    // mp.insert(make_pair(112,123));
    mp.insert({40,12});
    mp.insert({60,12});
    mp.insert({140,12});
    mp.insert({160,12});
    // cout << mp.first << mp[0].second << endl;
    return 0;
}
